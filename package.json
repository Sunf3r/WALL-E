{
	"module": "build/main.js",
	"type": "module",
	"exports": "./build/main.js",
	"engines": {
		"node": ">=20"
	},
	"scripts": {
		"setup": "npm install -g typescript pm2 prisma && npm run prisma:push && npm install && npm start",
		"update": "git pull origin master && npm update --save; deno upgrade & bun upgrade & npm run postinstall; npm run restart",
		"prisma:push": "npx prisma db push --schema=settings/schema.prisma",
		"postinstall": "rm package-lock.json & npm run prisma:gen && npm run build",
		"prisma:gen": "npx prisma generate --schema=settings/schema.prisma",
		"build": "rm -r build/ & tsc",
		"start": "pm2 start settings/ecosystem.config.cjs --attach",
		"restart": "pm2 restart walle --attach",
		"fmt": "deno fmt ./ --config=settings/deno.jsonc",
		"tsnd": "ts-node --esm main.ts",
		"clean": "rm -r settings/log/* settings/temp/*"
	},
	"dependencies": {
		"@google/generative-ai": "^0.16.0",
		"@prisma/client": "^5.17.0",
		"baileys": "^6.7.5",
		"chalk": "^5.3.0",
		"express": "^4.19.2",
		"google-sr": "^3.3.1",
		"google-translate": "github:iamtraction/google-translate",
		"googlethis": "^1.8.0",
		"i18next": "^23.12.2",
		"i18next-fs-backend": "^2.3.2",
		"luxon": "^3.4.4",
		"pino": "^9.3.2",
		"qrcode-terminal": "^0.12.0",
		"wa-sticker-formatter": "^4.4.4"
	},
	"devDependencies": {
		"@types/express": "^4.17.21",
		"@types/luxon": "^3.4.2",
		"@types/node": "^20.14.13",
		"prisma": "^5.17.0",
		"typescript": "^5.5.4"
	}
}
